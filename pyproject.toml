[project]
name = "microgrpc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "grpcio>=1.65",
  "grpcio-tools>=1.65",
  "pydantic>=2.7",
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "httpx>=0.28.1",
]

[tool.pyright]
pythonVersion = "3.11"

